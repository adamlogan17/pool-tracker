[project]
name = "pool-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Adam Logan", email = "adamlogan42@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "blinker>=1.9.0",
    "boto3>=1.43.1",
    "faker>=40.15.0",
    "mongoengine>=0.29.3",
]

[project.scripts]
pool-tracker = "pool_tracker:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
